{
  "name": "@allforai/ui-test-probe-cli",
  "version": "0.1.0",
  "description": "CLI tool for UI Test Probe — instrument, validate, and report on probe annotations",
  "bin": {
    "ui-test-probe": "./dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "glob": "^10.3.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
